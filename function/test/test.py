import numpy

a = numpy.array([[1, 2, 3], [1, 2, 3], [1, 2, 3]])
b = a
a = a[:, :-1]
b[0][0] = 0
# 赋值操作是针对内存区域的写入, 因此浅拷贝状态ab仍然是等价指针
# 切片操作是一个视图操作, 原有的内存区域没有变化, 但指针指向的区域变化了
print(a)
print(b)
